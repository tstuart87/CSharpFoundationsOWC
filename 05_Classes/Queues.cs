using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace _05_Classes
{
    public class QueueClass
    {
        //QUEUES
        //Count - return the total count of items in queue
        //Enqeueue(T) - Adds T to the queue
        //Dequeue() - Returns an item and removes it from queue
        //Peek() - Returns first item from queue does not remove it
        //Contains(T) - Checks whether T is in the queue or not
        //Clear() - clears the queue

        //STACKS
        //Count - return the total count of items in stack
        //Push(T) - Adds T to the stack
        //Pop() - Returns an item and removes it from stack
        //Peek() - Returns first item from stack does not remove it
        //Contains(T) - Checks whether T is in the stack or not
        //Clear() - clears the stack

        //DICTIONARIES
        //Add(K, P) - Adds K,P to dictionary
        //Remove(K) - Removes K,P from dictionary
        //ContainsKey(K) - Check whether K,P with key of K exists in dictionary
        //Clear() - clears the dictionary

        

    }

    public class StackClass
    {

    }

}


